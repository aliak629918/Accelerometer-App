import renderer from "react-test-renderer";
import App from "../App";

describe("App", () => {
  it("if there is no accelerometer data it should return with an error message", () => {
    expect();
  });
});
